# Avatar Preview Images

Add the same avatar images here for UI preview:
- `avatar1.jpg` through `avatar5.jpg` (must match server/assets/avatars/)
- Used to show thumbnail previews in the Avatar News tab UI

Note: Both server/assets/avatars/ and client/public/avatars/ must have matching avatar images.
